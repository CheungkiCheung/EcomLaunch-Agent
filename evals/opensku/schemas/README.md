# OpenSKU Artifact Contracts

This directory contains machine-readable contract notes used by `evals/opensku/validators`.

The Python validator is the executable source of truth. The JSON file documents the fields and section names that must remain stable for agent outputs and eval fixtures.

